#include "holberton.h"

/**
 * is_palindrome - checks to see if a string is a palindrome
 * Return: 1 if plaindrome else 0;
 * @s: string from main
 */

int is_palindrome(char *s)
{
	int n = 1;
	char *string = s;
	while (*string)
	{
		string++;
	}	
	return (n);
}
