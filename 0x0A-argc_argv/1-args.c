#include <stdio.h>

/**
 * main - prints number of arguments to command line
 * Return: 0
 * @argc: number of arguments
 * @argv: array of the arguments
 */

int main(int argc, char **argv)
{
	printf("%d\n", argc - 1);
	argv = argv;
	return (0);
}
